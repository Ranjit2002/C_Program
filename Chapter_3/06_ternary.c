#include <stdio.h>

int main()
{
    // Condition ? expression-if-true : expression-if-false

    int a = 365;
    int b = 57;

    a > b ? printf("a is greater") : printf("b is greater");

    return 0;
}