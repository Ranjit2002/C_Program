#include <stdio.h>

int main()
{
    // int a;
    // a = 4;

    int a = 4; // This is same as the upper one.

    // float b;
    // b = 1.5;

    float b = 1.5; // This is same as the upper one.

    // char c;
    // c = 'r';

    char c = 'r'; // This is same as the upper one.

    printf("The value of a is %d\n", a);
    printf("The value of b is %f\n", b);
    printf("The value of c is %c\n", c);

    return 0;
}